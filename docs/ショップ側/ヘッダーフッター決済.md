## テーマ

- テーマはlocalStorageに保存する。リロード時にチラつかせないこと。

## お気に入り、カート、決済

- お気に入り、カートはlocalstorageに保存する。
- ログイン機能は作らない。
- 決済には、Stripe Checkout hostedを使用する。
  - Stripeに進む前に、自前サイトのCheckout画面で配送先、配送方法、備考（日時指定など）を入力する。
  - 配送方法は、管理画面で設定した有効な配送方法のみ表示する。
  - 送料は、選択した配送方法の金額を注文に保存し、Stripeへは選択済み送料として渡す。
  - Stripe側では配送先入力・配送方法選択は行わず、決済だけを行う。
  - 商品情報を画像付きでStripe側に表示する。
  - Stripeから戻った際の画面は別途必要。
    - 成功時は注文内容を表示する為、session_idが必要。
    - /checkout/success?session_id={CHECKOUT_SESSION_ID}
    - /checkout/cancel
